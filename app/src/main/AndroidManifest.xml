<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gunsoft.aplikasiprobabilitaspelemparandaduatauuang" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MenuActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MataUangActivity"
            android:label="@string/title_activity_mata_uang" >
        </activity>
        <activity
            android:name=".MataDaduActivity"
            android:label="@string/title_activity_mata_dadu" >
        </activity>
    </application>

</manifest>
